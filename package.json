{
  "name": "ay-hub",
  "version": "0.1.0",
  "private": true,
  "description": "Painel pessoal e profissional para a AY Social Media.",
  "scripts": {
    "dev": "npm.cmd run open",
    "dev:hot": "concurrently \"npm.cmd run dev --prefix backend\" \"npm.cmd run dev --prefix frontend\"",
    "open": "npm.cmd run build --prefix frontend && npm.cmd start --prefix backend",
    "install:all": "npm.cmd install --prefix backend && npm.cmd install --prefix frontend",
    "build": "npm.cmd run build --prefix frontend",
    "start": "npm.cmd start --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
